<Window x:Class="ZMotionWpfControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="正运动控制卡运动程序"
        Width="1180"
        Height="760"
        MinWidth="980"
        MinHeight="640"
        Background="#F3F5F7"
        FontFamily="Microsoft YaHei UI">
  <Window.Resources>
    <Style x:Key="Panel" TargetType="Border">
      <Setter Property="Background" Value="White" />
      <Setter Property="BorderBrush" Value="#D8DEE8" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="6" />
      <Setter Property="Padding" Value="16" />
    </Style>
    <Style TargetType="Button">
      <Setter Property="Height" Value="36" />
      <Setter Property="Margin" Value="0,0,8,8" />
      <Setter Property="Padding" Value="12,0" />
      <Setter Property="Background" Value="#2364AA" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Cursor" Value="Hand" />
    </Style>
    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="Background" Value="#C1121F" />
      <Setter Property="Height" Value="44" />
      <Setter Property="FontSize" Value="16" />
    </Style>
    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="Background" Value="#5C6B73" />
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Height" Value="32" />
      <Setter Property="Padding" Value="8,4" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="BorderBrush" Value="#B9C3D0" />
    </Style>
    <Style TargetType="ComboBox">
      <Setter Property="Height" Value="32" />
      <Setter Property="Padding" Value="6,3" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
    <Style x:Key="LabelText" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#52616B" />
      <Setter Property="Margin" Value="0,0,0,6" />
    </Style>
    <Style x:Key="SectionTitle" TargetType="TextBlock">
      <Setter Property="FontSize" Value="16" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="#1F2A33" />
      <Setter Property="Margin" Value="0,0,0,14" />
    </Style>
  </Window.Resources>

  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <DockPanel Margin="0,0,0,14">
      <StackPanel DockPanel.Dock="Left">
        <TextBlock Text="正运动控制卡运动程序" FontSize="24" FontWeight="Bold" Foreground="#17202A" />
        <TextBlock Text="C# WPF 运动控制界面 / 预留真实控制卡 API 接入层" Foreground="#61717D" Margin="0,4,0,0" />
      </StackPanel>
      <Border DockPanel.Dock="Right"
              Background="#E9F5EE"
              BorderBrush="#9BD3AE"
              BorderThickness="1"
              CornerRadius="16"
              Padding="14,6"
              VerticalAlignment="Center">
        <TextBlock x:Name="ConnectionStateText" Text="未连接" Foreground="#216E3A" FontWeight="SemiBold" />
      </Border>
    </DockPanel>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="340" />
        <ColumnDefinition Width="14" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
        <StackPanel>
          <Border Style="{StaticResource Panel}" Margin="0,0,0,12">
            <StackPanel>
              <TextBlock Text="控制卡连接" Style="{StaticResource SectionTitle}" />
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="92" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                  <TextBlock Text="IP 地址" Style="{StaticResource LabelText}" />
                  <TextBox x:Name="IpAddressTextBox" Text="192.168.0.11" />
                </StackPanel>
                <StackPanel Grid.Column="1">
                  <TextBlock Text="端口" Style="{StaticResource LabelText}" />
                  <TextBox x:Name="PortTextBox" Text="8089" />
                </StackPanel>
              </Grid>
              <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                <Button x:Name="ConnectButton" Content="连接" Width="96" Click="ConnectButton_Click" />
                <Button x:Name="DisconnectButton" Content="断开" Width="96" Style="{StaticResource SecondaryButton}" Click="DisconnectButton_Click" />
              </StackPanel>
            </StackPanel>
          </Border>

          <Border Style="{StaticResource Panel}" Margin="0,0,0,12">
            <StackPanel>
              <TextBlock Text="轴控制" Style="{StaticResource SectionTitle}" />
              <TextBlock Text="当前轴" Style="{StaticResource LabelText}" />
              <ComboBox x:Name="AxisComboBox" DisplayMemberPath="Name" SelectedValuePath="Axis" SelectedIndex="0" />

              <UniformGrid Columns="2" Margin="0,14,0,0">
                <Button Content="伺服使能" Click="ServoOnButton_Click" />
                <Button Content="伺服断开" Style="{StaticResource SecondaryButton}" Click="ServoOffButton_Click" />
                <Button Content="回零" Click="HomeButton_Click" />
                <Button Content="清除报警" Style="{StaticResource SecondaryButton}" Click="ClearAlarmButton_Click" />
              </UniformGrid>

              <TextBlock Text="点动速度 mm/s" Style="{StaticResource LabelText}" Margin="0,8,0,6" />
              <TextBox x:Name="JogSpeedTextBox" Text="20" />
              <UniformGrid Columns="2" Margin="0,12,0,0">
                <Button Content="负向点动" PreviewMouseDown="JogNegativeButton_Down" PreviewMouseUp="JogButton_MouseUp" MouseLeave="JogButton_MouseLeave" />
                <Button Content="正向点动" PreviewMouseDown="JogPositiveButton_Down" PreviewMouseUp="JogButton_MouseUp" MouseLeave="JogButton_MouseLeave" />
              </UniformGrid>
            </StackPanel>
          </Border>

          <Border Style="{StaticResource Panel}" Margin="0,0,0,12">
            <StackPanel>
              <TextBlock Text="定位运动" Style="{StaticResource SectionTitle}" />
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                  <TextBlock Text="目标/距离 mm" Style="{StaticResource LabelText}" />
                  <TextBox x:Name="TargetPositionTextBox" Text="100" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                  <TextBlock Text="速度 mm/s" Style="{StaticResource LabelText}" />
                  <TextBox x:Name="MoveSpeedTextBox" Text="50" />
                </StackPanel>
              </Grid>
              <UniformGrid Columns="2" Margin="0,14,0,0">
                <Button Content="绝对运动" Click="MoveAbsoluteButton_Click" />
                <Button Content="相对运动" Click="MoveRelativeButton_Click" />
              </UniformGrid>
              <Button Content="单轴停止" Style="{StaticResource SecondaryButton}" Click="StopAxisButton_Click" />
              <Button Content="急停全部轴" Style="{StaticResource DangerButton}" Click="EmergencyStopButton_Click" />
            </StackPanel>
          </Border>
        </StackPanel>
      </ScrollViewer>

      <Grid Grid.Column="2">
        <Grid.RowDefinitions>
          <RowDefinition Height="2*" />
          <RowDefinition Height="12" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource Panel}">
          <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="轴状态监控" Style="{StaticResource SectionTitle}" />
            <DataGrid x:Name="AxisStatusGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      RowHeight="36"
                      IsReadOnly="True"
                      BorderThickness="0">
              <DataGrid.Columns>
                <DataGridTextColumn Header="轴号" Binding="{Binding Axis}" Width="70" />
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="90" />
                <DataGridTextColumn Header="位置 mm" Binding="{Binding Position, StringFormat=F3}" Width="*" />
                <DataGridTextColumn Header="速度 mm/s" Binding="{Binding Speed, StringFormat=F2}" Width="*" />
                <DataGridCheckBoxColumn Header="使能" Binding="{Binding Enabled}" Width="80" />
                <DataGridCheckBoxColumn Header="报警" Binding="{Binding Alarm}" Width="80" />
                <DataGridTextColumn Header="状态" Binding="{Binding State}" Width="120" />
              </DataGrid.Columns>
            </DataGrid>
          </DockPanel>
        </Border>

        <Grid Grid.Row="2">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <Border Style="{StaticResource Panel}">
            <StackPanel>
              <TextBlock Text="运行参数" Style="{StaticResource SectionTitle}" />
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="34" />
                  <RowDefinition Height="34" />
                  <RowDefinition Height="34" />
                  <RowDefinition Height="34" />
                </Grid.RowDefinitions>
                <TextBlock Text="加速度" Foreground="#52616B" />
                <TextBlock Grid.Column="1" Text="100 mm/s2" FontWeight="SemiBold" />
                <TextBlock Grid.Row="1" Text="减速度" Foreground="#52616B" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="100 mm/s2" FontWeight="SemiBold" />
                <TextBlock Grid.Row="2" Text="控制周期" Foreground="#52616B" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="1 ms" FontWeight="SemiBold" />
                <TextBlock Grid.Row="3" Text="插补模式" Foreground="#52616B" />
                <TextBlock Grid.Row="3" Grid.Column="1" Text="单轴/点动" FontWeight="SemiBold" />
              </Grid>
            </StackPanel>
          </Border>

          <Border Grid.Column="2" Style="{StaticResource Panel}">
            <DockPanel>
              <TextBlock DockPanel.Dock="Top" Text="运行日志" Style="{StaticResource SectionTitle}" />
              <ListBox x:Name="LogListBox" BorderThickness="0" Background="#FAFBFC" />
            </DockPanel>
          </Border>
        </Grid>
      </Grid>
    </Grid>
  </Grid>
</Window>
